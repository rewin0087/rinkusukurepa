require 'spec_helper'

describe Rinkusukurepa do
  it 'has a version number' do
    expect(Rinkusukurepa::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
